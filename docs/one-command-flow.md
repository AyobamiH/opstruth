# One Command Flow

`opstruth` runs repo, secrets, quality, conditional platform checks, conditional route/runtime checks, then evidence. It returns pass, warn, fail, skipped, or not_verified and separates verified facts from unverified claims.
