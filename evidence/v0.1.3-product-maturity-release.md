# opstruth v0.1.3 Product Maturity Release Evidence

Date: 2026-06-07

## Release Reason

`opstruth@0.1.3` publishes the product maturity work for public testing:

- stronger probe catalogue output
- ANSI-free `opstruth probes --json`
- clearer proof limitations and skipped-probe reasons
- expanded fixture matrix evidence
- safer configuration and runtime-input handling
- improved redacted secret finding context
- documentation for CI, evidence packs, Supabase, Cloudflare, and route/runtime proof gaps

## Published Version

- npm package: `opstruth@0.1.3`
- npm tarball: `https://registry.npmjs.org/opstruth/-/opstruth-0.1.3.tgz`
- GitHub tag: `v0.1.3`
- GitHub release: `https://github.com/AyobamiH/opstruth/releases/tag/v0.1.3`
- Release source commit: `6af2105 Bump opstruth CLI to v0.1.3`

## Package Checks

Commands run from `cli/`:

```bash
npm run lint
npm test
node bin/opstruth.js --help
node bin/opstruth.js welcome
node bin/opstruth.js probes
node bin/opstruth.js probes --json
node bin/opstruth.js --skip evidence
node bin/opstruth.js --json --skip evidence
node bin/opstruth.js --color --skip evidence
node bin/opstruth.js --no-color --skip evidence
NO_COLOR=1 node bin/opstruth.js --skip evidence
NPM_CONFIG_CACHE=/tmp/opstruth-npm-cache npm pack --dry-run
```

Results:

- lint passed
- tests passed
- help, welcome, probe catalogue, and one-command surfaces ran successfully
- `opstruth probes --json` parsed as JSON and was ANSI-free
- one-command JSON output completed with `status: warn`, no failures, and redacted warnings only
- color, no-color, and `NO_COLOR=1` terminal modes completed
- dry-run package contained 50 files and did not include generated website/build/cache artifacts

Known warning:

- `npm publish` warned that npm auto-corrected package metadata, but registry verification confirmed `bin.opstruth` remained `bin/opstruth.js`.

## Fixture Matrix

Command:

```bash
./scripts/run-fixture-matrix.sh
```

Result:

- fixture matrix completed successfully
- evidence directory: `evidence/fixture-matrix/`
- expected fixture behavior remained intact, including deliberate failing-test and risky-secret fixtures

## Local Install Test

Commands:

```bash
NPM_CONFIG_CACHE=/tmp/opstruth-npm-cache npm pack --pack-destination /tmp/opstruth-pack-0.1.3
npm install -g /tmp/opstruth-pack-0.1.3/opstruth-0.1.3.tgz --prefix=/tmp/opstruth-0.1.3-prefix
/tmp/opstruth-0.1.3-prefix/bin/opstruth --help
/tmp/opstruth-0.1.3-prefix/bin/opstruth welcome
/tmp/opstruth-0.1.3-prefix/bin/opstruth probes
/tmp/opstruth-0.1.3-prefix/bin/opstruth probes --json
/tmp/opstruth-0.1.3-prefix/bin/opstruth --skip evidence
/tmp/opstruth-0.1.3-prefix/bin/opstruth --json --skip evidence
```

Result:

- local tarball install passed
- installed binary resolved correctly from `/tmp`
- command surfaces ran successfully

## NPM Publish

Auth check:

```bash
npm whoami
```

Result:

- authenticated npm user: `oneclicksystems`
- `opstruth@0.1.3` published to npm with public access
- registry metadata verified version `0.1.3`, repository `AyobamiH/opstruth`, directory `cli`, and binary `opstruth`

## Public Install Test

Commands:

```bash
npm install -g opstruth@0.1.3 --prefix=/tmp/opstruth-public-0.1.3-prefix
/tmp/opstruth-public-0.1.3-prefix/bin/opstruth --help
/tmp/opstruth-public-0.1.3-prefix/bin/opstruth welcome
/tmp/opstruth-public-0.1.3-prefix/bin/opstruth probes --json
/tmp/opstruth-public-0.1.3-prefix/bin/opstruth --skip evidence
/tmp/opstruth-public-0.1.3-prefix/bin/opstruth --json --skip evidence
```

Result:

- public registry install passed
- installed binary ran successfully
- public one-command JSON output parsed with `status: warn`, zero failures, one expected no-git warning, and 17 verified facts in the empty temp project

## GitHub Release

Commands:

```bash
git tag v0.1.3
git push origin v0.1.3
gh release create v0.1.3 --repo AyobamiH/opstruth
```

Result:

- tag pushed to GitHub
- release created: `https://github.com/AyobamiH/opstruth/releases/tag/v0.1.3`

## What Was Not Done

- no Lovable setting changes
- no manual Cloudflare deploy
- no npm token printed
- no `.npmrc` written inside the repo
- no `.env`, token, tarball, node_modules, dist/build/cache, `.wrangler`, or temp auth files committed
- no video work
- no production architecture changes

## Remaining Proof Gaps

- Production website deployment was not manually triggered by this release task.
- Route checks still require `--base-url` or route config before public route availability is verified.
- Local runtime checks still require explicit local ports, health paths, processes, or services.
- Supabase and Cloudflare checks remain static unless explicit runtime/route evidence is provided.
