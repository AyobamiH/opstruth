# v0.1.1 npm release evidence

## Summary

- npm package name: `opstruth`
- npm version: `0.1.1`
- npm tarball: `https://registry.npmjs.org/opstruth/-/opstruth-0.1.1.tgz`
- GitHub tag: `v0.1.1`
- GitHub release: `https://github.com/AyobamiH/opstruth/releases/tag/v0.1.1`
- Production website: `https://opstruth.woeinvests.workers.dev`

## npm registry verification

`npm view opstruth@0.1.1 version bin dist.tarball --json` returned:

```json
{
  "version": "0.1.1",
  "bin": {
    "opstruth": "bin/opstruth.js"
  },
  "dist.tarball": "https://registry.npmjs.org/opstruth/-/opstruth-0.1.1.tgz"
}
```

## Public install smoke test

The published package was installed into a temporary global prefix from the npm registry.

Verified commands:

```bash
opstruth --help
opstruth welcome
opstruth probes
```

All three commands exited successfully from the temporary installed binary.

## Package checks before publish

The release flow verified:

- `npm run lint`
- `npm test`
- `node bin/opstruth.js --help`
- `node bin/opstruth.js welcome`
- `node bin/opstruth.js probes`
- `node bin/opstruth.js --skip evidence`
- npm pack inspection
- public npm install smoke test

`node bin/opstruth.js --skip evidence` reported expected redacted demo and fixture secret-reference warnings, with no failures.

## What was not done

- No Lovable deploy was run.
- No secrets were committed.
- No npm token was printed.
- No production architecture change was made.
- No extra npm publish, version bump, tag, or GitHub release was created after `v0.1.1`.

## Scope

`opstruth@0.1.1` is a v0.1 public testing release. It provides local, read-only operational truth checks, but it is not a maturity claim and does not replace security review, production observability, or environment-specific runtime verification.
