# v0.1 Release Readiness

Audit time: 2026-06-06T04:38:40Z

## Release Candidate

- Repository: `AyobamiH/opstruth`
- Visibility: public
- Branch: `main`
- Release candidate commit: `bfe188af6feca83951adc836dc8a38e0f0ed43c8`
- Production URL: `https://opstruth.woeinvests.workers.dev`
- Production runtime: Cloudflare Workers
- Lovable role: frontend editing/design surface only, not production deployment

## Production Verification

- `curl -I https://opstruth.woeinvests.workers.dev` returned `HTTP/2 200`.
- `curl -I https://opstruth.woeinvests.workers.dev/demo/opstruth-runtime-truth.mp4` returned `HTTP/2 200`.
- The production video response used `content-type: video/mp4`.
- Cloudflare served both responses.

## GitHub Actions

- Latest Cloudflare deploy workflow run: `27052551864`
- Status: success
- Trigger: workflow dispatch
- Result: Cloudflare deploy step ran and completed.
- Latest CI run after deployment docs update: `27052641705`
- CI status: success

## CLI Checks

Commands run from `cli/`:

```bash
npm install
npm run lint
npm test
node bin/opstruth.js --help
node bin/opstruth.js welcome
node bin/opstruth.js probes
node bin/opstruth.js secrets
node bin/opstruth.js --skip evidence
```

Results:

- `npm install` reported dependencies up to date with `0` vulnerabilities.
- `npm run lint` passed JavaScript syntax checks.
- `npm test` passed `1` Node test.
- `--help`, `welcome`, and `probes` completed successfully.
- `opstruth secrets` completed with `STATUS: Partial pass`, no failures, and redacted warnings only.
- `opstruth --skip evidence` completed with `STATUS: Partial pass`, no failures, and expected proof gaps for route/local runtime inputs.

## Website And Cloudflare Checks

Commands run from the repository root and `website/`:

```bash
npm run build
npx wrangler deploy --dry-run
cd website
npm run build
npm run lint
```

Results:

- Root build passed and synced `website/dist/client` to `dist/client`.
- Root build passed and synced `website/dist/server` to `dist/server`.
- Wrangler deploy dry run passed without deploying.
- Website build passed.
- Website lint returned `0` errors and `6` Fast Refresh warnings in existing UI component files.

## Video Asset

- Built asset path: `website/dist/client/demo/opstruth-runtime-truth.mp4`
- Built asset size: `3.6M`
- Production asset path: `/demo/opstruth-runtime-truth.mp4`
- Production asset status: `HTTP/2 200`

## Secret Safety

- No `.env` files are tracked.
- No `node_modules/` directories are tracked.
- No `dist/`, `build/`, `.wrangler/`, or cache output directories are intentionally staged for release.
- `.env`, `.env.*`, `node_modules/`, `dist/`, `build/`, `.wrangler/`, and `.cache/` are ignored by repository rules.
- opstruth secret scan printed redacted previews only.

Expected warning classes:

- Demo/video text references to `SUPABASE_SERVICE_ROLE_KEY` are placeholders used to demonstrate redaction behavior.
- Fixture app references such as `OPENAI_API_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, and bearer-style examples are intentionally unsafe fixtures for scanner coverage.
- Scanner/redaction source code contains the risky-reference patterns it detects.

## Known Limitations

- The package is not published to npm.
- Some opstruth probes are static-only.
- Production route checks require explicit `--base-url` input when running opstruth.
- Local runtime checks require explicit port, health path, process, or service inputs.
- opstruth does not prove production state unless production/staging route or runtime inputs are provided.
- Website lint still reports Fast Refresh warnings in UI component files, but no lint errors.
- Wrangler dry run reports known side-effect warnings from TanStack/H3 packages; the dry run still exits successfully.

## What Was Not Done

- No npm publish was performed.
- No GitHub release was created.
- No Lovable deployment dependency was introduced.
- No production architecture was changed.
- No Cloudflare production deploy was run during this release-readiness audit.
- No secrets were committed.
